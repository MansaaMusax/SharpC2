<Window x:Class="Client.Views.AddListenerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="Add Listener" Height="450" Width="500"
        WindowStartupLocation="CenterScreen">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0"/>
        <TextBox Text="{Binding ListenerName}" Grid.Row="0" Grid.Column="1"/>

        <TextBlock Text="Type:" Grid.Row="1" Grid.Column="0"/>
        <ComboBox ItemsSource="{Binding ListenerTypes}" SelectedItem="{Binding SelectedListener}" Grid.Row="1" Grid.Column="1"/>

        <ContentControl Content="{Binding NewListenerContent}" Grid.Row="2" Grid.Column="1" Margin="0 10 0 0"/>

        <Button Command="{Binding StartListener}" Content="Add" Grid.Column="1" Grid.Row="3"/>

    </Grid>
    
</Window>