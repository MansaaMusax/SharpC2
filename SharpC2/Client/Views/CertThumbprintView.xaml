<Window x:Class="Client.Views.CertThumbprintView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="Certificate Hash" Height="150" Width="500"
        WindowStartupLocation="CenterScreen">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding CertHash}" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Width="400"/>

        <WrapPanel Grid.Row="1" HorizontalAlignment="Center">
            <Button Content="Accept" Command="{Binding AcceptHash}"/>
            <Button Content="Reject" Command="{Binding RejectHash}"/>
        </WrapPanel>

    </Grid>

</Window>