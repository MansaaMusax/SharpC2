<Window x:Class="Client.Views.PowerShellPayloadView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="PowerShell Stager" Height="260" Width="800"
        WindowStartupLocation="CenterScreen">
    
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Launcher"/>
        <TextBox Grid.Row="1" Text="{Binding Launcher}" TextWrapping="NoWrap"/>

        <TextBlock Grid.Row="2" Text="Encoded Launcher"/>
        <TextBox Grid.Row="3" Text="{Binding EncodedLauncher}" TextWrapping="NoWrap"/>

        <Button Content="OK" Command="{Binding CloseView}" Grid.Column="2" Grid.Row="4"/>

    </Grid>
        
</Window>